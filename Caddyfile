# Caddy handles HTTPS + forwards everything to the frontend container,
# which in turn proxies /api/* to the backend via its own nginx config.
#
# Replace 'shivdiary.in' with your actual domain before deploying.

shivdiary.in, www.shivdiary.in {
    reverse_proxy frontend:80
    encode gzip zstd
}
